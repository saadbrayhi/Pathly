import {DiscoveryPage} from "@/components/discovery/discovery-page";import {discovery} from "@/data/discovery";export default function Page(){return <DiscoveryPage data={discovery['study-abroad']}/>}

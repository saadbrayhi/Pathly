import {FileCheck2,MessageSquareText,Route,type LucideIcon} from "lucide-react";import {ServiceRequest,note} from "@/components/forms/service-request";import {HeroBackdrop} from "@/components/visuals/hero-backdrop";
const process:[LucideIcon,string][]=[[MessageSquareText,"Share your goal"],[FileCheck2,"We review your file"],[Route,"Receive a clear plan"]];
export default function Page(){return <><section className="relative isolate overflow-hidden py-16 text-white"><HeroBackdrop dark/><div className="shell grid gap-10 lg:grid-cols-[1fr_.8fr] lg:items-center"><div><p className="eyebrow !text-blue-300">Pathly Plus</p><h1 className="mt-4 max-w-2xl text-4xl font-semibold tracking-[-.04em] sm:text-5xl">Let our team help carry the application.</h1><p className="mt-5 max-w-2xl leading-7 text-slate-300">Tell us what you need. We’ll review your situation first, then confirm exactly how we can help.</p></div><div className="grid gap-3 sm:grid-cols-3 lg:grid-cols-1">{process.map(([Icon,t])=><div key={t} className="flex items-center gap-3 rounded-xl border border-white/10 bg-white/10 p-4 backdrop-blur"><Icon className="text-blue-300" size={19}/><span className="text-sm font-medium">{t}</span></div>)}</div></div></section><section className="shell grid gap-8 py-16 lg:grid-cols-[.55fr_1fr]"><aside><p className="eyebrow">Request support</p><h2 className="mt-3 text-3xl font-semibold tracking-tight">Start with a review.</h2><p className="mt-4 text-sm leading-6 text-slate-500">Complete the form with as much detail as you can. The team will use it to understand your profile and requested service.</p><div className="mt-7 rounded-2xl border border-blue-200 bg-blue-50 p-5 text-sm leading-6 text-blue-900"><b>Important:</b><br/>{note}</div></aside><ServiceRequest/></section></>}

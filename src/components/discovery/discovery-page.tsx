"use client";
import {useMemo,useState} from "react"; import Image from "next/image"; import {ArrowRight,Clock,MapPin,SlidersHorizontal} from "lucide-react"; import type {DiscoveryPageData} from "@/types"; import {SearchBar} from "@/components/search/search-bar"; import {Button} from "@/components/ui/button"; import {HeroBackdrop} from "@/components/visuals/hero-backdrop";
export function DiscoveryPage({data}: {data:DiscoveryPageData}){const [query,setQuery]=useState("");const items=useMemo(()=>data.items.filter(i=>`${i.title} ${i.subtitle} ${i.location} ${i.tags.join(' ')}`.toLowerCase().includes(query.toLowerCase())),[data.items,query]);return <><section className="relative isolate overflow-hidden"><HeroBackdrop/><div className="shell py-16 text-center"><p className="eyebrow">{data.eyebrow}</p><h1 className="mx-auto mt-4 max-w-3xl text-4xl font-semibold tracking-[-.04em] sm:text-5xl">{data.title}</h1><p className="mx-auto mt-4 max-w-2xl leading-7 text-slate-600">{data.description}</p><div className="mx-auto mt-8 max-w-3xl"><SearchBar placeholder={data.placeholder} compact onSearch={setQuery}/></div></div></section><section className="shell py-12 pb-20"><div className="grid gap-7 lg:grid-cols-[240px_1fr]"><aside className="h-fit rounded-2xl border border-slate-200 bg-white p-5 shadow-card"><div className="mb-5 flex items-center gap-2 font-semibold"><SlidersHorizontal size={18}/>Filters</div><div className="space-y-5">{data.filters.map(f=><label key={f.label} className="block text-xs font-semibold text-slate-500">{f.label}<select className="field mt-2 !py-2.5 font-normal text-ink">{f.options.map(o=><option key={o}>{o}</option>)}</select></label>)}</div><button className="mt-5 w-full text-left text-xs font-semibold text-blue-600">Clear all filters</button></aside><div><div className="mb-4 flex items-center justify-between"><p className="text-sm text-slate-500"><b className="text-ink">{items.length}</b> opportunities found</p><select className="rounded-lg border border-slate-200 px-3 py-2 text-xs"><option>Most relevant</option><option>Deadline soon</option></select></div><div className="space-y-4">{items.map((i,idx)=><article key={i.title} className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-card transition hover:border-blue-200"><div className="grid sm:grid-cols-[210px_1fr]">{i.image&&<div className="relative min-h-48 bg-slate-100 sm:min-h-full"><Image src={i.image} alt={i.title} fill sizes="(max-width:640px) 100vw, 210px" className="object-cover"/></div>}<div className="p-5 sm:p-6"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-start"><div><div className="flex flex-wrap items-center gap-2">{i.featured&&<span className="rounded-full bg-blue-50 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-blue-700">Featured</span>}<span className="text-xs text-slate-400">#{String(idx+1).padStart(2,'0')}</span></div><h2 className="mt-3 text-lg font-semibold">{i.title}</h2><p className="mt-1 text-sm text-slate-500">{i.subtitle}</p><div className="mt-4 flex flex-wrap gap-2">{i.tags.map(t=><span key={t} className="rounded-lg bg-slate-100 px-2.5 py-1 text-xs text-slate-600">{t}</span>)}</div></div><Button variant="secondary" className="shrink-0">View details <ArrowRight size={15}/></Button></div><div className="mt-5 flex flex-wrap gap-5 border-t border-slate-100 pt-4 text-xs text-slate-500"><span className="flex items-center gap-1.5"><MapPin size={14}/>{i.location}</span>{i.deadline&&<span className="flex items-center gap-1.5"><Clock size={14}/>Deadline {i.deadline}</span>}</div></div></div></article>)}{items.length===0&&<div className="rounded-2xl border border-dashed border-slate-300 py-16 text-center text-slate-500">No results yet. Try a broader search.</div>}</div></div></div></section></>}

"use client";
import { FormEvent,useState } from "react"; import { Search,Sparkles } from "lucide-react"; import { useRouter } from "next/navigation"; import { cn } from "@/lib/utils";
export function SearchBar({placeholder="Search opportunities, countries, or questions…",compact=false,onSearch}:{placeholder?:string;compact?:boolean;onSearch?:(q:string)=>void}){const [q,setQ]=useState("");const router=useRouter();function submit(e:FormEvent){e.preventDefault();onSearch?.(q)}return <form onSubmit={submit} className={cn("glass flex w-full items-center gap-2 rounded-2xl p-2",!compact&&"shadow-[0_22px_70px_rgba(20,80,170,.16)]")}><Search className="ml-3 shrink-0 text-slate-400" size={20}/><input value={q} onChange={e=>setQ(e.target.value)} className="min-w-0 flex-1 bg-transparent px-1 py-3 text-sm outline-none placeholder:text-slate-400 sm:text-base" placeholder={placeholder}/><button type="button" onClick={()=>router.push(`/ai-guide?q=${encodeURIComponent(q)}`)} className="inline-flex min-h-11 shrink-0 items-center gap-2 rounded-xl bg-blue-600 px-4 text-sm font-semibold text-white hover:bg-blue-700"><Sparkles size={16}/><span className="hidden sm:inline">Ask AI Guide</span><span className="sm:hidden">Ask AI</span></button></form>}
